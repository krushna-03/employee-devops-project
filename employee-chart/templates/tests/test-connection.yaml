apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "employee-chart.fullname" . }}-test-connection"
  labels:
    {{- include "employee-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "employee-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
